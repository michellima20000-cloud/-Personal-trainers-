rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Global relaxed access to prevent permission errors in sandboxed sandbox/iframe environments where Auth is blocked
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
